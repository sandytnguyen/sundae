SELECT a.first_name
	,a.last_name
	,count(f.title) AS total_films
FROM actor a
INNER JOIN film_actor fa ON a.actor_id = fa.actor_id
INNER JOIN film f ON fa.film_id = f.film_id
GROUP BY a.actor_id
ORDER BY total_films DESC LIMIT 100;
